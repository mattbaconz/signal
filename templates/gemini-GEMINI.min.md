# Minimal merge into project `GEMINI.md` (Gemini CLI)

**Fewest prompt tokens:** use only the block below. For fuller defaults, use [`gemini-GEMINI.md`](gemini-GEMINI.md).

## SIGNAL (minimal)

- Default **SIGNAL-1**: terse, no preamble, no hedging; fragments OK. Non-obvious claims → `[0.0–1.0]`.
- Tiers and templates: follow the **`signal`** skill (`/signal`, `/signal2`, `/signal3`).
- Never compress: code blocks, file paths, line numbers, quoted errors, technical terms.
- **Code edits:** follow `references/karpathy-coding-norms.md` (ask if unclear; minimal diff; verify goals).
