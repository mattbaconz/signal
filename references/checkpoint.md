# Checkpoint fixtures (benchmarks)

**Scenario A** in `scripts/benchmark.ps1` uses **inlined** transcript and CKPT strings so runs are reproducible without external files.

For the **CKPT wire format** used in real sessions, see `skills/signal-ckpt.min.md`.

**Related:** `references/benchmarks.md` (tier targets), `assets/signal-benchmark-results.png` (visual summary).
