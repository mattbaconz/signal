# Chess equal — verbose control

Paired harness arm: project **`GEMINI.md`** asks for **full sentences** (not SIGNAL).

The **Style** section differs from [`chess equal signal`](../chess%20equal%20signal/README.md); the **Preservation** section is **byte-aligned** so both arms load similar on-disk prompt bulk. Use with `run_chess_compare.ps1 -Pair EqualContext`.
