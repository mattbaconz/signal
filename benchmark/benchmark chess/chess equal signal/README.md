# Chess equal — SIGNAL

Paired harness arm: project **`GEMINI.md`** asks for **SIGNAL-1** (terse, skill-backed).

The **Style** section differs from [`chess equal verbose`](../chess%20equal%20verbose/README.md); the **Preservation** section matches for prompt-size parity. Use with `run_chess_compare.ps1 -Pair EqualContext`.
